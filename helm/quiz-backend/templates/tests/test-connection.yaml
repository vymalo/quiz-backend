apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quiz-backend.fullname" . }}-test-connection"
  labels:
    {{- include "quiz-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quiz-backend.fullname" . }}:{{ .Values.service.ports.http.port }}']
  restartPolicy: Never